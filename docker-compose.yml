services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data

  scheduler:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    command: crond -f -l 2
